CREATE TABLE login (
    id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(50) NOT NULL
);

INSERT INTO login (id, username, password)
VALUES (1, 'Prerna_Jha', 'Pjha');
